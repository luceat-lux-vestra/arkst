[package]
name = "markdown-maintained-substrate-check"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
rushdown = { version = "=0.18.0", default-features = false, features = ["std", "html-entities"] }
