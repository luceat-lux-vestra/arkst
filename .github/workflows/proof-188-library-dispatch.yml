name: proof-188-library-dispatch

on:
  push:
    branches:
      - proof/188-library-dispatch

permissions:
  contents: write

jobs:
  proof:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Apply bounded product patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace(path, old, new, expected=1):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != expected:
                  raise SystemExit(f"{path}: expected {expected} occurrence(s), found {count}: {old[:100]!r}")
              p.write_text(text.replace(old, new))

          # SourceStore: reserve provenance IDs without making library sources path resources.
          replace(
              "crates/arkst-project/src/source_store.rs",
              "    /// Adds a source to the store, assigning a new SourceId.\n",
              "    /// Reserves a deterministic SourceId for semantic source provenance that is\n"
              "    /// intentionally not addressable through the project path namespace.\n"
              "    ///\n"
              "    /// The caller owns lookup for the detached source. The reserved ID is never\n"
              "    /// reused by a later path-backed source.\n"
              "    pub(crate) fn allocate_detached_id(&mut self) -> Result<SourceId, SourceStoreError> {\n"
              "        self.allocate_id()\n"
              "    }\n\n"
              "    /// Adds a source to the store, assigning a new SourceId.\n",
          )

          # VirtualProject: explicit in-memory, case-sensitive loadable-library registry.
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "use arkst_source::SourceId;\n",
              "use arkst_source::SourceId;\nuse std::collections::BTreeMap;\nuse std::sync::Arc;\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "/// A compilation project with all sources and assets in memory.\n",
              "/// One loadable Quarkdown library supplied by the host as immutable in-memory source.\n"
              "///\n"
              "/// Library names are semantic registry keys, not virtual paths. Their source IDs\n"
              "/// are deterministic provenance identities allocated after ordinary project sources.\n"
              "#[derive(Debug, Clone)]\n"
              "pub struct LoadableLibrary {\n"
              "    name: String,\n"
              "    source_id: SourceId,\n"
              "    source: Arc<str>,\n"
              "}\n\n"
              "impl LoadableLibrary {\n"
              "    pub fn name(&self) -> &str {\n"
              "        &self.name\n"
              "    }\n\n"
              "    pub fn source_id(&self) -> SourceId {\n"
              "        self.source_id\n"
              "    }\n\n"
              "    pub fn source(&self) -> &str {\n"
              "        self.source.as_ref()\n"
              "    }\n"
              "}\n\n"
              "/// A compilation project with all sources and assets in memory.\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "pub struct VirtualProject {\n    entry: VirtualPathBuf,\n    sources: SourceStore,\n    assets: AssetStore,\n    metadata: ProjectMetadata,\n}\n",
              "pub struct VirtualProject {\n"
              "    entry: VirtualPathBuf,\n"
              "    sources: SourceStore,\n"
              "    assets: AssetStore,\n"
              "    metadata: ProjectMetadata,\n"
              "    loadable_libraries: BTreeMap<String, LoadableLibrary>,\n"
              "    library_names_by_source_id: BTreeMap<u32, String>,\n"
              "}\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "        assets: AssetStore,\n        metadata: ProjectMetadata,\n    ) -> Self {\n        Self {\n            entry,\n            sources,\n            assets,\n            metadata,\n        }\n",
              "        assets: AssetStore,\n"
              "        metadata: ProjectMetadata,\n"
              "        loadable_libraries: BTreeMap<String, LoadableLibrary>,\n"
              "        library_names_by_source_id: BTreeMap<u32, String>,\n"
              "    ) -> Self {\n"
              "        Self {\n"
              "            entry,\n"
              "            sources,\n"
              "            assets,\n"
              "            metadata,\n"
              "            loadable_libraries,\n"
              "            library_names_by_source_id,\n"
              "        }\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "    /// Resolves a local logical resource relative to the source document that\n",
              "    /// Looks up a host-supplied loadable library by its exact, case-sensitive name.\n"
              "    pub fn loadable_library(&self, name: &str) -> Option<&LoadableLibrary> {\n"
              "        self.loadable_libraries.get(name)\n"
              "    }\n\n"
              "    /// Returns the semantic library name associated with a detached provenance ID.\n"
              "    pub fn loadable_library_name_by_source_id(&self, source_id: SourceId) -> Option<&str> {\n"
              "        self.library_names_by_source_id\n"
              "            .get(&source_id.0)\n"
              "            .map(String::as_str)\n"
              "    }\n\n"
              "    /// Resolves a local logical resource relative to the source document that\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "    assets: Vec<(VirtualPathBuf, Vec<u8>)>,\n    metadata: ProjectMetadata,\n",
              "    assets: Vec<(VirtualPathBuf, Vec<u8>)>,\n"
              "    metadata: ProjectMetadata,\n"
              "    loadable_libraries: Vec<(String, String)>,\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "    /// Adds an asset (font, image, etc.).\n",
              "    /// Adds a loadable Quarkdown library to the semantic registry.\n"
              "    ///\n"
              "    /// Names are matched exactly and case-sensitively by `.include` before file\n"
              "    /// lookup. Validation for empty or duplicate names happens atomically in `build`.\n"
              "    pub fn add_loadable_library(\n"
              "        mut self,\n"
              "        name: impl Into<String>,\n"
              "        content: impl Into<String>,\n"
              "    ) -> Self {\n"
              "        self.loadable_libraries.push((name.into(), content.into()));\n"
              "        self\n"
              "    }\n\n"
              "    /// Adds an asset (font, image, etc.).\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "        // Sort assets by canonical path for deterministic iteration\n",
              "        // Allocate library provenance after all ordinary sources so adding a\n"
              "        // library never changes existing project SourceId assignments.\n"
              "        let mut libraries = self.loadable_libraries;\n"
              "        libraries.sort_by(|a, b| a.0.cmp(&b.0));\n"
              "        let mut loadable_libraries = BTreeMap::new();\n"
              "        let mut library_names_by_source_id = BTreeMap::new();\n"
              "        for (name, source) in libraries {\n"
              "            if name.is_empty() {\n"
              "                return Err(BuildError::EmptyLoadableLibraryName);\n"
              "            }\n"
              "            if loadable_libraries.contains_key(&name) {\n"
              "                return Err(BuildError::DuplicateLoadableLibrary(name));\n"
              "            }\n"
              "            let source_id = source_store.allocate_detached_id().map_err(|error| match error {\n"
              "                SourceStoreError::SourceIdExhausted => BuildError::SourceIdExhausted,\n"
              "                SourceStoreError::DuplicateSource(_) => {\n"
              "                    unreachable!(\"detached SourceId allocation does not insert a path\")\n"
              "                }\n"
              "            })?;\n"
              "            library_names_by_source_id.insert(source_id.0, name.clone());\n"
              "            loadable_libraries.insert(\n"
              "                name.clone(),\n"
              "                LoadableLibrary {\n"
              "                    name,\n"
              "                    source_id,\n"
              "                    source: Arc::from(source),\n"
              "                },\n"
              "            );\n"
              "        }\n\n"
              "        // Sort assets by canonical path for deterministic iteration\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "            asset_store,\n            self.metadata,\n        ))\n",
              "            asset_store,\n"
              "            self.metadata,\n"
              "            loadable_libraries,\n"
              "            library_names_by_source_id,\n"
              "        ))\n",
          )
          replace(
              "crates/arkst-project/src/virtual_project.rs",
              "    #[error(\"duplicate asset: {0}\")]\n    DuplicateAsset(VirtualPathBuf),\n",
              "    #[error(\"duplicate asset: {0}\")]\n"
              "    DuplicateAsset(VirtualPathBuf),\n"
              "    #[error(\"loadable library name must not be empty\")]\n"
              "    EmptyLoadableLibraryName,\n"
              "    #[error(\"duplicate loadable library: {0}\")]\n"
              "    DuplicateLoadableLibrary(String),\n",
          )

          # Engine semantic library provider remains separate from path resources.
          replace(
              "crates/arkst-engine/src/lib.rs",
              "/// Logical root requested by a resource-aware evaluator operation.\n",
              "/// Immutable source returned by an explicit loadable-library registry.\n"
              "#[derive(Debug, Clone, PartialEq, Eq)]\n"
              "pub struct LoadableLibrarySource {\n"
              "    pub name: String,\n"
              "    pub source_id: arkst_source::SourceId,\n"
              "    pub text: String,\n"
              "}\n\n"
              "/// Semantic lookup for host-supplied loadable libraries.\n"
              "///\n"
              "/// This is intentionally distinct from `ResourceProvider`: a bare `.include`\n"
              "/// argument is first matched exactly against this registry and only falls back\n"
              "/// to logical project-path resolution when no library exists.\n"
              "pub trait LoadableLibraryProvider {\n"
              "    fn loadable_library(&self, name: &str) -> Option<LoadableLibrarySource>;\n"
              "}\n\n"
              "/// Logical root requested by a resource-aware evaluator operation.\n",
          )

          # Evaluator wiring and library-first dispatch.
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              "    Capabilities, Capability, EvaluationLimits, IncludedSource, ResourceAccessError,\n    ResourceProvider, ResourceRoot, ResourceText,\n",
              "    Capabilities, Capability, EvaluationLimits, IncludedSource, LoadableLibraryProvider,\n"
              "    LoadableLibrarySource, ResourceAccessError, ResourceProvider, ResourceRoot, ResourceText,\n",
          )
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              "    resources: Option<&'a dyn ResourceProvider>,\n    metadata_defaults: crate::DocumentMetadataDefaults,\n",
              "    resources: Option<&'a dyn ResourceProvider>,\n"
              "    loadable_libraries: Option<&'a dyn LoadableLibraryProvider>,\n"
              "    metadata_defaults: crate::DocumentMetadataDefaults,\n",
          )
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              "            resources: None,\n            metadata_defaults: crate::DocumentMetadataDefaults::default(),\n",
              "            resources: None,\n"
              "            loadable_libraries: None,\n"
              "            metadata_defaults: crate::DocumentMetadataDefaults::default(),\n",
              expected=2,
          )
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              "            resources: self.resources,\n            metadata_defaults: self.metadata_defaults.clone(),\n",
              "            resources: self.resources,\n"
              "            loadable_libraries: self.loadable_libraries,\n"
              "            metadata_defaults: self.metadata_defaults.clone(),\n",
              expected=2,
          )
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              "    fn source_mode(&self, source_id: SourceId) -> Option<Mode> {\n",
              "    fn with_resources_and_libraries(\n"
              "        resources: &'a dyn ResourceProvider,\n"
              "        loadable_libraries: &'a dyn LoadableLibraryProvider,\n"
              "        source_id: SourceId,\n"
              "        source_mode: Mode,\n"
              "        metadata_defaults: &crate::DocumentMetadataDefaults,\n"
              "        limits: EvaluationLimits,\n"
              "    ) -> Self {\n"
              "        let mut context = Self::with_resources(\n"
              "            resources,\n"
              "            source_id,\n"
              "            source_mode,\n"
              "            metadata_defaults,\n"
              "            limits,\n"
              "        );\n"
              "        context.loadable_libraries = Some(loadable_libraries);\n"
              "        context\n"
              "    }\n\n"
              "    fn source_mode(&self, source_id: SourceId) -> Option<Mode> {\n",
          )
          old_method = '''    pub fn evaluate_with_resources_for_mode<R: ResourceProvider>(
        &self,
        resources: &R,
        source_id: SourceId,
        source_mode: Mode,
        document: &IrDocument,
        metadata_defaults: &crate::DocumentMetadataDefaults,
    ) -> (IrDocument, Vec<Diagnostic>) {
        let mut diagnostics = Vec::new();
        let mut context = EvaluationContext::with_resources(
            resources,
            source_id,
            source_mode,
            metadata_defaults,
            self.limits,
        );
        self.evaluate_with_context(document, &mut diagnostics, &mut context)
    }
'''
          new_method = old_method + '''
    /// Evaluates resource-backed IR with an explicit, separate loadable-library registry.
    pub fn evaluate_with_resources_and_libraries_for_mode<
        R: ResourceProvider,
        L: LoadableLibraryProvider,
    >(
        &self,
        resources: &R,
        loadable_libraries: &L,
        source_id: SourceId,
        source_mode: Mode,
        document: &IrDocument,
        metadata_defaults: &crate::DocumentMetadataDefaults,
    ) -> (IrDocument, Vec<Diagnostic>) {
        let mut diagnostics = Vec::new();
        let mut context = EvaluationContext::with_resources_and_libraries(
            resources,
            loadable_libraries,
            source_id,
            source_mode,
            metadata_defaults,
            self.limits,
        );
        self.evaluate_with_context(document, &mut diagnostics, &mut context)
    }
'''
          replace("crates/arkst-engine/src/evaluator.rs", old_method, new_method)
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              "    fn evaluate_include_reference(\n",
              '''    fn evaluate_loadable_library(
        &self,
        requested_name: &str,
        library: LoadableLibrarySource,
        span: &SourceSpan,
        diagnostics: &mut Vec<Diagnostic>,
        context: &mut EvaluationContext<'_>,
    ) -> CallOutcome {
        if library.name != requested_name {
            diagnostics.push(resource_diagnostic(
                "E9001",
                format!(
                    "loadable-library provider returned `{}` for requested name `{requested_name}`",
                    library.name
                ),
                *span,
                "A LoadableLibraryProvider must preserve exact, case-sensitive registry identity.",
            ));
            return CallOutcome::Failed;
        }

        let mode = Mode::Quarkdown;
        if let Err(existing_mode) = context.register_source_mode(library.source_id, mode) {
            diagnostics.push(resource_diagnostic(
                "E9001",
                format!(
                    "loadable library `{requested_name}` parser-mode provenance conflict for source identity {:?}: {existing_mode:?} vs {mode:?}",
                    library.source_id
                ),
                *span,
                "A LoadableLibraryProvider must map one library SourceId to one canonical Quarkdown source within an evaluation.",
            ));
            return CallOutcome::Failed;
        }

        let diagnostics_start = diagnostics.len();
        let parsed = arkst_markdown::parse_with_mode(&library.text, mode);
        for diagnostic in parsed.diagnostics {
            diagnostics.push(Diagnostic {
                code: diagnostic.code.to_string(),
                severity: Severity::Error,
                message: diagnostic.message,
                primary: Some(SourceSpan {
                    source_id: library.source_id,
                    start: diagnostic.span.start,
                    end: diagnostic.span.end,
                }),
                secondary: Vec::new(),
                hints: Vec::new(),
            });
        }
        if diagnostics.len() != diagnostics_start {
            return CallOutcome::Failed;
        }
        let (document, lowering_diagnostics) = ast_to_ir::ast_to_ir_with_diagnostics_for_mode(
            &parsed.document,
            library.source_id,
            &context.metadata_defaults,
            mode,
        );
        diagnostics.extend(lowering_diagnostics);
        if diagnostics.len() != diagnostics_start {
            return CallOutcome::Failed;
        }

        // QdLibraryExporter evaluates library source directly in the caller context.
        // Do not change current_source or apply the requested include sandbox: resource
        // accesses retain the includer's base and declarations are shared back.
        let result = self.evaluate_nodes(&document.nodes, diagnostics, context);
        if diagnostics.len() != diagnostics_start {
            return CallOutcome::Failed;
        }
        CallOutcome::Value(IrValue::Content(result))
    }

    fn evaluate_include_reference(
''',
          )
          replace(
              "crates/arkst-engine/src/evaluator.rs",
              '''    ) -> CallOutcome {
        if reject_host_filesystem_reference("include", &reference, *span, diagnostics) {
''',
              '''    ) -> CallOutcome {
        if let Some(loadable_libraries) = context.loadable_libraries {
            if let Some(library) = loadable_libraries.loadable_library(&reference) {
                return self.evaluate_loadable_library(
                    &reference,
                    library,
                    span,
                    diagnostics,
                    context,
                );
            }
        }
        if reject_host_filesystem_reference("include", &reference, *span, diagnostics) {
''',
          )

          # Core adapter supplies both independent semantic interfaces from VirtualProject.
          replace(
              "crates/arkst-core/src/engine_adapter.rs",
              "    DocumentMetadataDefaults, IncludedSource, ResourceAccessError, ResourceProvider,\n    ResourceRoot as EngineResourceRoot, ResourceText,\n",
              "    DocumentMetadataDefaults, IncludedSource, LoadableLibraryProvider, LoadableLibrarySource,\n"
              "    ResourceAccessError, ResourceProvider, ResourceRoot as EngineResourceRoot, ResourceText,\n",
          )
          replace(
              "crates/arkst-core/src/engine_adapter.rs",
              '''    fn source_path(&self, source_id: SourceId) -> Option<String> {
        self.project
            .sources()
            .path_by_id(source_id)
            .map(ToString::to_string)
    }
''',
              '''    fn source_path(&self, source_id: SourceId) -> Option<String> {
        self.project
            .sources()
            .path_by_id(source_id)
            .map(ToString::to_string)
            .or_else(|| {
                self.project
                    .loadable_library_name_by_source_id(source_id)
                    .map(|name| format!("@library/{name}"))
            })
    }
''',
          )
          replace(
              "crates/arkst-core/src/engine_adapter.rs",
              "fn map_resource_error(error: ProjectResourceAccessError) -> ResourceAccessError {\n",
              '''impl LoadableLibraryProvider for VirtualProjectResourceProvider<'_> {
    fn loadable_library(&self, name: &str) -> Option<LoadableLibrarySource> {
        let library = self.project.loadable_library(name)?;
        Some(LoadableLibrarySource {
            name: library.name().to_string(),
            source_id: library.source_id(),
            text: library.source().to_string(),
        })
    }
}

fn map_resource_error(error: ProjectResourceAccessError) -> ResourceAccessError {
''',
          )
          replace(
              "crates/arkst-core/src/lib.rs",
              "        pub(crate) fn evaluate_with_resources<R: arkst_engine::ResourceProvider>(\n",
              "        pub(crate) fn evaluate_with_resources<\n"
              "            R: arkst_engine::ResourceProvider + arkst_engine::LoadableLibraryProvider,\n"
              "        >(\n",
          )
          replace(
              "crates/arkst-core/src/lib.rs",
              "            self.inner.evaluate_with_resources_for_mode(\n                resources,\n                source_id,\n",
              "            self.inner.evaluate_with_resources_and_libraries_for_mode(\n"
              "                resources,\n"
              "                resources,\n"
              "                source_id,\n",
          )

          Path("crates/arkst-project/tests/loadable_library_registry.rs").write_text(r'''use arkst_project::{BuildError, VirtualProjectBuilder};

fn project_with_libraries(order: &[&str]) -> arkst_project::VirtualProject {
    let mut builder = VirtualProjectBuilder::new()
        .entry("docs/main.qd")
        .expect("entry")
        .add_source("docs/main.qd", "main")
        .expect("main")
        .add_source("docs/nested.qd", "nested")
        .expect("nested");
    for name in order {
        builder = builder.add_loadable_library(*name, format!("source-{name}"));
    }
    builder.build().expect("project")
}

#[test]
fn registry_is_exact_case_sensitive_and_does_not_change_project_source_ids() {
    let baseline = project_with_libraries(&[]);
    let project = project_with_libraries(&["zeta", "alpha"]);

    for path in ["docs/main.qd", "docs/nested.qd"] {
        let path = arkst_project::VirtualPathBuf::parse(path).unwrap();
        assert_eq!(baseline.sources().get_id(&path), project.sources().get_id(&path));
    }

    let alpha = project.loadable_library("alpha").expect("alpha");
    let zeta = project.loadable_library("zeta").expect("zeta");
    assert_eq!(alpha.source(), "source-alpha");
    assert!(project.loadable_library("Alpha").is_none());
    assert!(alpha.source_id().0 < zeta.source_id().0, "sorted names get deterministic IDs");
    assert_eq!(
        project.loadable_library_name_by_source_id(alpha.source_id()),
        Some("alpha")
    );
    assert!(project.sources().get_by_id(alpha.source_id()).is_none());
}

#[test]
fn registry_rejects_empty_and_duplicate_names_atomically() {
    let empty = VirtualProjectBuilder::new()
        .entry("main.qd")
        .unwrap()
        .add_source("main.qd", "main")
        .unwrap()
        .add_loadable_library("", "x")
        .build();
    assert!(matches!(empty, Err(BuildError::EmptyLoadableLibraryName)));

    let duplicate = VirtualProjectBuilder::new()
        .entry("main.qd")
        .unwrap()
        .add_source("main.qd", "main")
        .unwrap()
        .add_loadable_library("hello", "one")
        .add_loadable_library("hello", "two")
        .build();
    assert!(matches!(
        duplicate,
        Err(BuildError::DuplicateLoadableLibrary(name)) if name == "hello"
    ));
}
''')

          Path("crates/arkst-engine/tests/loadable_library_dispatch.rs").write_text(r'''use arkst_engine::{
    ast_to_ir::ast_to_ir_with_diagnostics_for_mode, DocumentMetadataDefaults, IncludedSource,
    LoadableLibraryProvider, LoadableLibrarySource, ResourceAccessError, ResourceProvider,
    ResourceText,
};
use arkst_ir::{IrDocument, IrInline, IrNode};
use arkst_markdown::Mode;
use arkst_source::SourceId;
use std::cell::RefCell;
use std::collections::{BTreeMap, HashMap};

#[derive(Default)]
struct FakeEnvironment {
    paths: HashMap<SourceId, String>,
    sources: HashMap<(SourceId, String), IncludedSource>,
    text: HashMap<(SourceId, String), Result<ResourceText, ResourceAccessError>>,
    libraries: BTreeMap<String, LoadableLibrarySource>,
    source_requests: RefCell<Vec<(SourceId, String)>>,
    text_requests: RefCell<Vec<(SourceId, String)>>,
    library_requests: RefCell<Vec<String>>,
}

impl ResourceProvider for FakeEnvironment {
    fn source_path(&self, source_id: SourceId) -> Option<String> {
        self.paths.get(&source_id).cloned().or_else(|| {
            self.libraries
                .values()
                .find(|library| library.source_id == source_id)
                .map(|library| format!("@library/{}", library.name))
        })
    }

    fn read_text(
        &self,
        source_id: SourceId,
        reference: &str,
    ) -> Result<ResourceText, ResourceAccessError> {
        self.text_requests
            .borrow_mut()
            .push((source_id, reference.to_string()));
        self.text
            .get(&(source_id, reference.to_string()))
            .cloned()
            .unwrap_or_else(|| Err(ResourceAccessError::NotFound {
                path: reference.to_string(),
            }))
    }

    fn read_source(
        &self,
        source_id: SourceId,
        reference: &str,
    ) -> Result<IncludedSource, ResourceAccessError> {
        self.source_requests
            .borrow_mut()
            .push((source_id, reference.to_string()));
        self.sources
            .get(&(source_id, reference.to_string()))
            .cloned()
            .ok_or_else(|| ResourceAccessError::NotFound {
                path: reference.to_string(),
            })
    }
}

impl LoadableLibraryProvider for FakeEnvironment {
    fn loadable_library(&self, name: &str) -> Option<LoadableLibrarySource> {
        self.library_requests.borrow_mut().push(name.to_string());
        self.libraries.get(name).cloned()
    }
}

fn document(source: &str, source_id: SourceId) -> IrDocument {
    let parsed = arkst_markdown::parse_with_mode(source, Mode::Quarkdown);
    assert!(parsed.diagnostics.is_empty(), "{:?}", parsed.diagnostics);
    let (document, diagnostics) = ast_to_ir_with_diagnostics_for_mode(
        &parsed.document,
        source_id,
        &DocumentMetadataDefaults::default(),
        Mode::Quarkdown,
    );
    assert!(diagnostics.is_empty(), "{diagnostics:?}");
    document
}

fn evaluate(source: &str, source_id: SourceId, env: &FakeEnvironment) -> (IrDocument, Vec<arkst_diagnostics::Diagnostic>) {
    arkst_engine::evaluator::Evaluator::new().evaluate_with_resources_and_libraries_for_mode(
        env,
        env,
        source_id,
        Mode::Quarkdown,
        &document(source, source_id),
        &DocumentMetadataDefaults::default(),
    )
}

fn paragraph_text(document: &IrDocument) -> String {
    document
        .nodes
        .iter()
        .filter_map(|node| match node {
            IrNode::Paragraph { content, .. } => Some(
                content
                    .iter()
                    .filter_map(|inline| match inline {
                        IrInline::Text { content, .. } => Some(content.as_str()),
                        _ => None,
                    })
                    .collect::<String>(),
            ),
            _ => None,
        })
        .collect::<Vec<_>>()
        .join("\n")
}

#[test]
fn exact_library_name_precedes_file_lookup_and_ignores_sandbox() {
    let main = SourceId(1);
    let fallback = SourceId(2);
    let library = SourceId(100);
    let mut env = FakeEnvironment::default();
    env.paths.insert(main, "main.qd".into());
    env.paths.insert(fallback, "Hello".into());
    env.sources.insert(
        (main, "hello".into()),
        IncludedSource { path: "hello".into(), source_id: fallback, text: "wrong file".into() },
    );
    env.sources.insert(
        (main, "Hello".into()),
        IncludedSource { path: "Hello".into(), source_id: fallback, text: "file fallback".into() },
    );
    env.libraries.insert(
        "hello".into(),
        LoadableLibrarySource {
            name: "hello".into(),
            source_id: library,
            text: ".var {libvalue} {library}".into(),
        },
    );

    let (result, diagnostics) = evaluate(
        ".include {hello} sandbox:{subdocument}\n.libvalue",
        main,
        &env,
    );
    assert!(diagnostics.is_empty(), "{diagnostics:?}");
    assert!(env.source_requests.borrow().is_empty());
    assert_eq!(env.library_requests.borrow().as_slice(), &["hello"]);
    assert_eq!(paragraph_text(&result), "library");

    env.source_requests.borrow_mut().clear();
    env.library_requests.borrow_mut().clear();
    let (result, diagnostics) = evaluate(".include {Hello}", main, &env);
    assert!(diagnostics.is_empty(), "{diagnostics:?}");
    assert_eq!(env.library_requests.borrow().as_slice(), &["Hello"]);
    assert_eq!(env.source_requests.borrow().as_slice(), &[(main, "Hello".into())]);
    assert_eq!(paragraph_text(&result), "file fallback");
}

#[test]
fn library_resource_access_keeps_includer_base_and_library_span_provenance() {
    let main = SourceId(11);
    let reader = SourceId(110);
    let broken = SourceId(111);
    let mut env = FakeEnvironment::default();
    env.paths.insert(main, "docs/main.qd".into());
    env.text.insert(
        (main, "data.txt".into()),
        Ok(ResourceText { path: "docs/data.txt".into(), text: "caller data".into() }),
    );
    env.libraries.insert(
        "reader".into(),
        LoadableLibrarySource { name: "reader".into(), source_id: reader, text: ".read {data.txt}".into() },
    );
    env.libraries.insert(
        "broken".into(),
        LoadableLibrarySource { name: "broken".into(), source_id: broken, text: ".read {missing.txt}".into() },
    );

    let (result, diagnostics) = evaluate(".include {reader} sandbox:{scope}", main, &env);
    assert!(diagnostics.is_empty(), "{diagnostics:?}");
    assert_eq!(env.text_requests.borrow().as_slice(), &[(main, "data.txt".into())]);
    assert_eq!(paragraph_text(&result), "caller data");

    env.text_requests.borrow_mut().clear();
    let (_, diagnostics) = evaluate(".include {broken}", main, &env);
    assert_eq!(diagnostics.len(), 1, "{diagnostics:?}");
    assert_eq!(env.text_requests.borrow().as_slice(), &[(main, "missing.txt".into())]);
    assert_eq!(diagnostics[0].primary.map(|span| span.source_id), Some(broken));
}

#[test]
fn includeall_uses_library_dispatch_per_element_then_file_fallback() {
    let main = SourceId(21);
    let local = SourceId(22);
    let library = SourceId(120);
    let mut env = FakeEnvironment::default();
    env.paths.insert(main, "main.qd".into());
    env.paths.insert(local, "local.qd".into());
    env.sources.insert(
        (main, "local.qd".into()),
        IncludedSource { path: "local.qd".into(), source_id: local, text: ".libvalue".into() },
    );
    env.libraries.insert(
        "hello".into(),
        LoadableLibrarySource {
            name: "hello".into(),
            source_id: library,
            text: ".var {libvalue} {library}".into(),
        },
    );

    let (result, diagnostics) = evaluate(
        ".includeall\n    - hello\n    - local.qd\n.libvalue",
        main,
        &env,
    );
    assert!(diagnostics.is_empty(), "{diagnostics:?}");
    assert_eq!(env.library_requests.borrow().as_slice(), &["hello", "local.qd"]);
    assert_eq!(env.source_requests.borrow().as_slice(), &[(main, "local.qd".into())]);
    assert!(paragraph_text(&result).contains("library"));
}
''')

          Path("crates/arkst-core/tests/loadable_library_compile.rs").write_text(r'''use arkst_core::{compile, CompileOptions, VirtualProjectBuilder};
use arkst_core::ir::{IrInline, IrNode};

fn paragraph_text(document: &arkst_core::ir::IrDocument) -> String {
    document
        .nodes
        .iter()
        .filter_map(|node| match node {
            IrNode::Paragraph { content, .. } => Some(
                content
                    .iter()
                    .filter_map(|inline| match inline {
                        IrInline::Text { content, .. } => Some(content.as_str()),
                        _ => None,
                    })
                    .collect::<String>(),
            ),
            _ => None,
        })
        .collect::<Vec<_>>()
        .join("\n")
}

#[test]
fn compile_wires_virtual_project_library_registry_before_local_file_lookup() {
    let project = VirtualProjectBuilder::new()
        .entry("main.qd")
        .unwrap()
        .add_source("main.qd", ".include {hello} sandbox:{scope}\n.libvalue")
        .unwrap()
        .add_source("hello", "wrong file")
        .unwrap()
        .add_loadable_library("hello", ".var {libvalue} {library}")
        .build()
        .unwrap();

    let result = compile(&project, &CompileOptions::default());
    assert!(result.diagnostics.is_empty(), "{:?}", result.diagnostics);
    assert_eq!(paragraph_text(&result.ir), "library");
}

#[test]
fn compile_library_reads_from_the_includer_resource_base() {
    let project = VirtualProjectBuilder::new()
        .entry("docs/main.qd")
        .unwrap()
        .add_source("docs/main.qd", ".include {reader} sandbox:{subdocument}")
        .unwrap()
        .add_asset("docs/data.txt", b"caller data".to_vec())
        .unwrap()
        .add_loadable_library("reader", ".read {data.txt}")
        .build()
        .unwrap();

    let result = compile(&project, &CompileOptions::default());
    assert!(result.diagnostics.is_empty(), "{:?}", result.diagnostics);
    assert_eq!(paragraph_text(&result.ir), "caller data");
}
''')
          PY

          cargo fmt --all
          git diff --check
          git rm .github/workflows/proof-188-library-dispatch.yml
      - name: Validate exact patched tree
        shell: bash
        run: |
          cargo test --locked -p arkst-project --test loadable_library_registry
          cargo test --locked -p arkst-engine --test loadable_library_dispatch
          cargo test --locked -p arkst-engine --test resource_contract
          cargo test --locked -p arkst-core --test loadable_library_compile
          cargo test --locked -p arkst-core --test engine_facade
          cargo clippy --locked -p arkst-project -p arkst-engine -p arkst-core --all-targets --all-features -- -D warnings
          git diff --check
      - name: Commit proven product tree
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/arkst-project/src/source_store.rs \
                  crates/arkst-project/src/virtual_project.rs \
                  crates/arkst-project/tests/loadable_library_registry.rs \
                  crates/arkst-engine/src/lib.rs \
                  crates/arkst-engine/src/evaluator.rs \
                  crates/arkst-engine/tests/loadable_library_dispatch.rs \
                  crates/arkst-core/src/engine_adapter.rs \
                  crates/arkst-core/src/lib.rs \
                  crates/arkst-core/tests/loadable_library_compile.rs \
                  .github/workflows/proof-188-library-dispatch.yml
          git commit -m "compat(resource): dispatch loadable libraries before files"
          git push origin HEAD:proof/188-library-dispatch
