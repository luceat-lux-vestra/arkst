# Quarkdown v2.5.1 Programmable-Language Semantics Audit

## Review record

- **Issue:** [#150](https://github.com/luceat-lux-vestra/scribium/issues/150)
- **Parent tracker:** [#147](https://github.com/luceat-lux-vestra/scribium/issues/147)
- **Target:** Quarkdown `v2.5.1`
- **Resolved upstream tag:** [`v2.5.1`](https://github.com/iamgio/quarkdown/releases/tag/v2.5.1)
- **Pinned upstream commit:** [`107ec3a9482f10d6f90d7580f8409b46a719d18e`](https://github.com/iamgio/quarkdown/tree/107ec3a9482f10d6f90d7580f8409b46a719d18e)
- **Scribium audit base:** [`489c7765bc28a74160b354d3237c7710c7eb4294`](https://github.com/luceat-lux-vestra/scribium/tree/489c7765bc28a74160b354d3237c7710c7eb4294)
- **Review date:** 2026-08-25

This is the canonical #150 inventory. It audits the programmable semantic
surface above raw call grammar; it does not implement the gaps it records.
The #61 closure and PR #131 established a bounded programmable-document
foundation, not complete v2.5.1 compatibility. The current matrix therefore
classifies every row against the upstream observable contract instead of
promoting an existing Scribium implementation merely because the #61 checklist
passed.

## Method and evidence boundary

The audit used only permitted clean-room evidence:

1. the public v2.5.1 wiki/API documentation;
2. public v2.5.1 source/API declarations pinned to the full commit above;
3. independently authored black-box probes against the installed official
   `quarkdown version 2.5.1` release binary;
4. current Scribium implementation, tests, fixtures, ADRs, and compatibility
   documents; and
5. live issue/PR history for ownership and prior evidence.

No Quarkdown source, internal test, or fixture was copied or translated. The
black-box probes were small documents authored for this audit and compared
observable output and failure effects. Upstream source links below are
provenance and behavioral evidence, not implementation instructions.

The primary status is exactly one #147 vocabulary value per inventory row:

`SUPPORTED_END_TO_END`, `SUPPORTED_SEMANTICS`, `PARSED_ONLY`, `PARTIAL`,
`UNSUPPORTED`, `DEFERRED`, `BLOCKED`, `NOT_APPLICABLE`, or `UNKNOWN`.

`PARTIAL` is used when a bounded semantic slice is evidenced but the public
surface or observable contract is broader. `SUPPORTED_SEMANTICS` is reserved
for semantic behavior whose runtime contract is evidenced independently of a
complete rendered-document claim. No #150 row is promoted to
`SUPPORTED_END_TO_END` by this audit.

## Pipeline and ownership boundary

The upstream call pipeline is represented by the pinned
[`FunctionCallRefiner.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/parser/FunctionCallRefiner.kt),
[`FunctionCallNodeExpander.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/call/FunctionCallNodeExpander.kt),
and [`NodeOutputValueVisitor.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/output/node/NodeOutputValueVisitor.kt).
Scribium keeps the corresponding semantic ownership in the single
`scribium-engine` evaluator: AST-to-IR conversion, evaluation, builtins,
conversion, normalization, `CallOutcome`, callable capture, and source-backed
diagnostics. `scribium-core` remains the composition/facade layer. The
backend-neutral `IrDocument` and typed `IrValue`/`IrComponent` boundary do not
acquire evaluator-specific or Typst-specific semantics in this audit.

The #149 value/binding/conversion audit is a referenced ownership boundary,
not a second #150 inventory. Where a call's parameter behavior affects
programmable semantics, the flow is recorded as:

`call evaluation -> #149 parameter binding/conversion -> evaluator scope and owner write-back`.

Document-state mutation is also kept separate. The shared evaluator-owned
`DocumentState` handle is noted only where it affects scope; metadata getter,
setter, append/replace, serde, and frontmatter behavior belong to [#152](https://github.com/luceat-lux-vestra/scribium/issues/152).

## Canonical semantic inventory

| Upstream surface | Upstream evidence | Observable contract | Evaluation contract | Scope ownership | State behavior | Failure behavior | Parser/frontend | Binder/conversion (#149) | Evaluator | IR | Backend | Diagnostics | Conformance | Current status | Existing issue/PR | Remaining gap |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Variables: declaration, lookup, assignment, reassignment, absence | [Variables wiki](https://quarkdown.com/wiki/variables/); pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt) | `.var {name} {value}` declares; `.name` reads; `.name {value}` reassigns; unresolved references fail rather than silently becoming supported values. | RHS is evaluated before the write; reads see the nearest visible binding; nested calls observe writes made before their evaluation. | Upstream searches the owner context for reassignment. Scribium assigns an existing real owner, while a new name is local to the current child context. Parameters/captures/lookup overlays are not owners. | Current Scribium callable success relays assigned existing owners; ordinary local declarations stay local. Shared document state is separate. | Upstream probes preserve a prior mutation before a later body failure; Scribium's callable/iteration paths intentionally roll back semantic variable publication on failure. | Existing variable/call parsing and source spans are reused; no grammar change. | Binding and origin-aware conversion are #149-owned; no builtin-local conversion is added here. | `EvaluationContext::assign_value`, owner search, `CallOutcome`, and callable commit path provide the bounded behavior. | Values remain typed `IrValue`; no variable semantics are put into IR. | No backend-specific variable behavior; only materialized IR is lowered. | Existing unresolved, assignment, failure, and span assertions are source-backed and deterministic. | `compile_callable_var_*`, variable and nested-scope tests; conformance scalar/numeric cases. | `PARTIAL` | #61/#131 foundation; #149/#165–#167 for binding/conversion; #150 audit. | Full v2.5.1 owner and failure-effect parity is not established; mutation-before-failure needs an ordered implementation decision after #156. |
| Scope: lexical capture, caller visibility, callable-local, parameter, nested and iteration scope | [Lambda wiki](https://quarkdown.com/wiki/lambda/); pinned [`Lambda.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/data/Lambda.kt) | A callable retains definition context, sees permitted caller context, installs invocation parameters, and isolates locals. Shadowing is observable by lookup precedence. | Definition capture is restored first, caller-visible bindings are overlaid, a child invocation context is created, and parameters are installed last. Nested calls repeat the same ordering. | Scribium's bounded hybrid is explicit: definition capture -> caller overlay -> invocation child -> parameter scope. Caller overlay forwards real owners but is not itself an owner. Iteration uses an isolated child. | Existing caller-owner mutation can be written back after successful callable evaluation; parameter and capture bindings do not overwrite their owners. | Nested failure produces one source-backed failure and does not publish the failed child state under Scribium's current atomicity invariant. | Frontend retains callable/body provenance; no parser scope shortcut. | Parameter shadowing and body fallback are linked to #149; #163 preserves argument shape for the binder. | `capture_snapshot`, `from_capture`, `with_caller_overlay`, child contexts, and `apply_callable_assignment` implement the bounded model. | `IrCallableCapture` and `IrValue::Callable` retain only backend-neutral callable data. | No backend scope semantics. | Scope failures retain the original call/body span in existing diagnostics. | `compile_captured_callable_*`, `compile_nested_callable_*`, `compile_invocation_parameters_shadow_*`, scope/owner tests. | `PARTIAL` | ADR-0020; #61/#131; #149/#165; [#163](https://github.com/luceat-lux-vestra/scribium/issues/163). | Broader mutable definition/caller context parity and all public callable consumers remain unverified. |
| Callable definitions, invocation, body results, inline/block bodies, and recursion | [Lambda wiki](https://quarkdown.com/wiki/lambda/); pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt) | `.function` defines a callable; invocation returns scalar, content, or no value according to the body. A bounded recursive callable is publicly observable and terminates when its condition reaches the base case. | Arguments are evaluated/bound before body evaluation; body statements stop at the first failure; recursive calls use the same callable path and depth limit. | Definition capture and caller overlay follow the scope contract above; body locals are invocation-local. | Successful typed/content result materializes through the one evaluator and IR; no-value is distinct from failure. | Failed body/call does not become a successful output; current Scribium atomicity is stricter than upstream's observed partial mutation. | Existing `FunctionCallRefiner`-compatible frontend and body spans are used; no new syntax. | Explicit/implicit parameters, optionality, and content/body conversion remain #149-owned. | `invoke_callable`, `invoke_bound_callable`, `evaluate_user_function`, and recursion depth guard are shared. | `IrValue::Callable`, `IrValue::Content`, and typed scalar values carry results. | Existing Typst lowering is used only after IR materialization; no callable semantics in Typst. | Existing body failure and result diagnostics preserve call source spans. | New `compile_recursive_user_function_uses_shared_callable_path`; nested/chain/body-result tests; #61 evidence. | `PARTIAL` | #61/#131; #63 corpus; #150. | General public callable/component surface and output-equivalent coverage remain incomplete; recursion has one bounded independent Scribium fixture, not full end-to-end promotion. |
| Parameters: explicit, implicit, optional/default, body fallback, shadowing, destructuring, callback-like values | [`FunctionParameter.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/call/FunctionParameter.kt); pinned [`Lambda.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/data/Lambda.kt); [`RegularArgumentsBinder.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/call/binding/RegularArgumentsBinder.kt) | Named and positional parameters bind to declared names; implicit references are positional; optional values may be absent; block bodies/callback values are lazy where the target declares them; Pair destructuring is shape-sensitive. | Normal argument expressions evaluate in source order, binding validates before the body, and lazy body/callback values evaluate only when invoked. | Parameters shadow caller and definition bindings but are never reassignment owners. Explicit parameters precede implicit fallback in lookup. | A successful bind installs values in the invocation child; conversion is target-driven and happens before callable execution. | Duplicate/unknown/excess/missing or incompatible values fail before body execution; conversion/body rollback boundaries are #149/#167. | Argument shape and provenance remain in #148 grammar/frontend work; #163 defers positional-after-named validity to the binder. | This row intentionally references #149/#165–#167 for binding, dynamic/content conversion, diagnostics, and commit atomicity. | Shared binder/evaluator path handles current explicit/implicit/optional/body slices. | `IrValue::Callable`, `IrValue::InlineBody`, `IrValue::Pair`, collections, and typed values are backend-neutral carriers. | No backend-specific parameter behavior. | Current parameter and conversion diagnostics retain call spans; generic wrapper normalization remains a #167 gap. | Optionality, Pair, nested conversion, body, and source-defined callable tests; `VALUE_MODEL_AUDIT.md`. | `PARTIAL` | [#149](https://github.com/luceat-lux-vestra/scribium/issues/149); [#165](https://github.com/luceat-lux-vestra/scribium/issues/165); [#166](https://github.com/luceat-lux-vestra/scribium/issues/166); [#167](https://github.com/luceat-lux-vestra/scribium/issues/167); #163. | Do not create a second binder/conversion implementation in #150; complete target-driven body/callback and diagnostic parity remains assigned to the adjacent workstream. |
| Evaluation order, nested calls, body evaluation, condition evaluation, and laziness | [Conditional statements](https://quarkdown.com/wiki/conditional-statements/); pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt) | Callee resolution precedes binding; ordinary arguments are source ordered; selected conditional/body/callback branches evaluate lazily; unselected branches are not evaluated. | Scribium's documented order is callee -> argument evaluation -> binding/conversion -> capture/overlay/child -> parameter installation -> lazy body -> typed result -> materialization. | Evaluation uses the active child/caller/definition layers; mutations become visible to later evaluation according to the state policy rather than eager pre-execution. | The evaluator shares document state but keeps invocation variable maps isolated; no hidden global state. | Nested failure propagates as `CallOutcome::Failed` and later independent document content can continue; state publication follows current atomicity. | Existing frontend call nodes preserve nested source boundaries; no semantic shortcut is moved into the parser. | #149 conversion occurs between argument evaluation and body invocation. | `evaluate_call_value_with_first_origin`, `evaluate_value`, conditional handlers, and one evaluator dispatch preserve the bounded order. | Nested values are normalized into one backend-neutral IR. | Backend lowering observes only the resulting IR; no Typst evaluation order is used as language evidence. | Deterministic source-backed diagnostics exist for bounded failure paths. | Conditional laziness, nested/chain, body-stops-after-failure, and deterministic compilation tests. | `PARTIAL` | ADR-0020; #150; #149/#167. | Full public lazy/eager matrix, including every callback/body target and conversion-origin variant, needs independent conformance coverage. |
| Chaining and call outcomes: scalar/content/no-value/unresolved/failed flow | [Syntax of a function call](https://quarkdown.com/wiki/syntax-of-a-function-call/); pinned call-expansion sources above and [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt) | `a::b` passes the preceding result into the next call; nested and chained forms are expected to agree for equivalent values; no-value, unresolved, and failure are distinct observable outcomes. | Each chain segment evaluates before the next; a failed/non-value segment cannot be treated as an ordinary successful input; later top-level content may still be evaluated after a chain failure. | The same caller/definition/child ownership applies to every segment; a stateful segment must expose its mutation according to the state policy before the next segment. | Typed scalar/content values flow through one IR; no-value and failed outcomes do not fabricate a value. | Existing Scribium distinguishes `Value`, `NoValue`, `Failed`, and `Unresolved`; chain parse-boundary failures remain #164. | Grammar/boundary and inline structure are #148/#157–#164; this row does not re-audit parser acceptance. | Segment binding/conversion is #149-owned and must not be duplicated in chain dispatch. | Chain dispatch and ordinary nested calls share `CallOutcome` classification. | `IrValue` carries only successful backend-neutral values; outcome state remains evaluator control flow. | No backend chain semantics. | Nested chain failures preserve the originating call span in current tests. | `compile_final_chain_no_value_is_legal_but_non_final_is_not`, nested no-value, chain failure, unresolved-chain, scalar nested/chain tests. | `PARTIAL` | [#164](https://github.com/luceat-lux-vestra/scribium/issues/164); #149/#165–#167; #61/#131. | Full source-defined/native and stateful chain equivalence, content-flow, and upstream failure-effect matrix is not complete. |
| Conditionals: truth contract, selected-branch laziness, nested conditions, branch mutation | [Conditional statements](https://quarkdown.com/wiki/conditional-statements/); pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt) | `.if` invokes its body only for a truthy Boolean; `.ifnot` negates it; `None` follows the public false/true contract; an unselected failing body has no observable failure. | Condition is evaluated before branch selection; selected body is lazy and nested conditions follow the same rule. | Branch bodies use the current invocation/caller ownership and isolate local bindings; branch mutation is visible only according to successful state publication. | Scribium's branch result participates in ordinary value/no-value/content materialization. | Selected-branch failure propagates; unselected branch failure is not evaluated. Current rollback is stricter than upstream partial-mutation probes. | Existing conditional grammar and spans are reused. | Boolean/None conversion and callback/body binding link to #149; no new truth conversion. | Existing conditional handlers are in the single evaluator. | Branch results are ordinary `IrValue`/content. | No backend conditional semantics. | Current branch diagnostics preserve selected-body source spans. | `compile_chain_and_ordinary_conditional_are_equally_lazy` and conditional failure/laziness tests. | `PARTIAL` | #61/#131; #149; #150. | Broader truth coercion, optionality interaction, mutation/failure parity, and complete nested conditional corpus remain open. |
| Iteration: iterable timing, loop ownership/scope, ordering, empty values, Pair destructuring, nested loops, body failure | [Loops](https://quarkdown.com/wiki/loops/); pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt), [`Lambda.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/data/Lambda.kt), and [`Range.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/data/Range.kt) | `.foreach` evaluates an iterable and maps ordered elements through a body; `.repeat` is range-backed iteration; empty input produces an empty result; explicit headers can destructure Pair; loop variables are iteration-local unless an existing owner is intentionally updated. | Iterable expression is evaluated before the body; items run in order; each body invocation gets the item binding; nested loops use nested child scopes. | Loop variable belongs to the iteration child; outer variables are shadowed for lookup; Scribium's bounded owner writeback covers existing caller owners after successful evaluation. | Current `.foreach`/`.repeat` supports bounded ranges, ordered collections, Pair, explicit/implicit forms, and typed/content result collection. | Upstream black-box failure preserves mutation from the failing iteration; Scribium currently restores failed iteration/callable state. Endless/general iterable and all callback targets are not claimed. | Existing iteration syntax/provenance is used; parser gaps remain #148. | Iterable/Pair/body conversion and destructuring boundary link #149/#166. | Shared iteration evaluator and limits enforce bounded depth/materialization. | Ordered `IrValue::Collection`, Pair, and content nodes; no iteration semantics in backend IR. | No backend iteration semantics. | Body failure retains source-backed diagnostics; deterministic item ordering is tested. | `compile_inline_foreach_failure_is_atomic_and_source_backed`, foreach/repeat source shadowing, Pair/range, order, empty, nested and limit tests; #63 corpus. | `PARTIAL` | #61/#131; #149/#166/#167; #150. | Upstream partial-effects contract, broader iterable forms, endless/range edges, arbitrary callbacks, and complete output conformance remain gaps. |
| Collection-oriented language semantics: selectors, sorting, filtering/mapping boundary, callback invocation | [Iterable](https://quarkdown.com/wiki/iterable/); pinned [`Collection.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Collection.kt), [`IterableValue.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/IterableValue.kt), and [`Lambda.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-core/src/main/kotlin/com/quarkdown/core/function/value/data/Lambda.kt) | Collection operations preserve ordered values and define one-based selectors, sorting/grouping/reversal/aggregation, and callback value flow. Public v2.5.1 `Collection.kt` does not itself establish `.map`/`.filter` as public collection functions; those are Scribium extension behavior and a separate boundary. | Selector/sort callback arguments evaluate under normal lazy callable rules; collection operations consume ordered values and return ordered values. | Callback scope follows callable capture/caller overlay; iteration-local values do not become global owners. | Current collection operations are typed and bounded; `.map`/`.filter` are explicitly not counted as upstream conformance. | Invalid selectors, callback failures, and conversion failures are source-backed/atomic under current Scribium policy; full upstream operation failure effects are not established here. | Call syntax belongs to #148; no collection parser is added. | Target conversions/callback body values link #149/#166; general builtin ownership is #151. | Existing engine collection handlers use one evaluator and shared conversion helpers. | Ordered `IrValue::Collection`, Pair, Dictionary, and scalar values. | No backend collection semantics unless a later content consumer materializes output. | Existing collection diagnostics and deterministic ordering tests. | Collection selector/sorted/group/reversed/Pair tests; `VALUE_MODEL_AUDIT.md`; #151 is the complete stdlib inventory. | `PARTIAL` | [#151](https://github.com/luceat-lux-vestra/scribium/issues/151); #149/#166; #150. | Keep #151 as the builtin inventory owner; remaining callback, transform, and full upstream operation parity must not be split into per-builtin issues. |
| Definition precedence: source-defined callable, native builtin, collisions, nested fallback | Pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt); pinned call-expansion sources above | A source-defined declaration can shadow a same-name native call where the public contract allows it; the result is observable in direct and chained invocation. Some document-state/native exceptions are intentionally native-first in current Scribium and must be compared separately. | Resolution precedes argument evaluation; nested resolution uses the active definition/caller precedence. | Definition capture is a fallback, caller-defined functions are visible through the caller overlay, and parameters have the highest local lookup precedence. | Source-defined callable registration changes the active function environment only; document-state names use the separate #152 policy. | Duplicate/invalid definitions and unresolved calls fail deterministically; fallback behavior is not silently assumed. | Parser only recognizes the call/definition shape; precedence is evaluator-owned. | Binding/conversion follows the selected callable and remains #149-owned. | Dispatch contains explicit source-defined/native ordering and name-local document-state exceptions. | Callable capture and function environment stay backend-neutral. | No backend precedence semantics. | Existing name-collision and unresolved-call diagnostics retain source spans. | `compile_user_functions_use_source_order_and_override_builtins`, source-defined foreach/repeat direct/chain tests, captured caller-function tests. | `PARTIAL` | #61/#131; #150; #151/#152 boundaries. | Complete v2.5.1 precedence table, every native collision, and extension-chain precedence are not yet evidenced; `.extend` is tracked separately below. |
| Extension/super-like semantics: `.extend`, optional `where`, target parameters, parent/original resolution, chained extensions | [Extending functions](https://quarkdown.com/wiki/extending-functions/); pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt) and [`FunctionExtension.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/internal/FunctionExtension.kt) | v2.5.1 exposes `.extend {target}`; an optional condition delegates to the prior target when false; an extension body receives `.super`, whose arguments can be overridden; extensions can chain around the original callable. A black-box probe produced `HELLO WORLD` and conditional `HELLO WORLD` / `Hello Other`. | Target resolution and condition evaluation precede extension body; `.super` delegates to the previous/original callable with merged arguments; nested/chained calls preserve normal call order. | Extension body should use the target definition/caller contract and expose parent resolution without making `.super` a global builtin. Parameter names must match the target contract. | No current Scribium evaluator path implements extension registration or `.super`; no state behavior is claimed. | Known public behavior is absent rather than partially simulated; failure and mutation semantics need an implementation-specific conformance slice. | Existing parser may preserve unknown calls, but parser acceptance is not semantic support; #148 owns only shape/provenance. | Target parameters and conditional/body conversion link #149/#165–#167; no duplicate binder. | No approved `extendFunction` equivalent exists in the current single evaluator. | No extension-specific IR is justified by this audit; preserve one IR unless an accepted architecture decision says otherwise. | No backend evidence is applicable before evaluator semantics exist. | No Scribium extension diagnostics/conformance exists; upstream target absence and mismatch cases must be added with source spans later. | Independent upstream black-box probe; current `rg`/evaluator review confirms no implementation. | `UNSUPPORTED` | New bounded follow-up [#169](https://github.com/luceat-lux-vestra/scribium/issues/169); implementation ordering frozen until #156. | Implement one engine-owned extension/super slice only after reconciliation; include target absence, parameter matching, condition, chain, scope, failure, atomicity, and diagnostics. |
| Failure propagation, rollback boundary, partial effects, nested/chain/iteration/body failure | Pinned [`Flow.kt`](https://github.com/iamgio/quarkdown/blob/107ec3a9482f10d6f90d7580f8409b46a719d18e/quarkdown-stdlib/src/main/kotlin/com/quarkdown/stdlib/Flow.kt); independent official-binary probes | A failed call reports failure, does not fabricate a value, and failure can occur inside a nested call, chain, callback, conditional, or iteration. Upstream probes show mutation before a later body/iteration failure remains observable (`after`, then `2`). | Already-evaluated arguments/body/iterations are not retroactively unevaluated by upstream; the exact commit boundary is observable. Scribium currently validates/commits bounded semantic state atomically in several paths. | Failure does not change the ownership model; it determines whether assigned owners publish. | Current `map_callable_values`/iteration and callable paths snapshot/restore on failure; conversion uses validate-then-commit boundaries. | The upstream-vs-Scribium effect difference is a compatibility gap, not an implementation authorization. Nested failure keeps source-backed diagnostics and later independent top-level evaluation. | Existing failure spans are retained; no grammar changes. | Conversion failure and diagnostics/commit boundaries are #149/#167; this row records only the programmable consequence. | `CallOutcome::Failed`, snapshots, owner application, depth/materialization limits, and one dispatch path are the current evidence. | Failed calls do not enter successful IR; successful prior document output remains deterministic. | No backend-specific rollback. | Current diagnostics are deterministic and source-backed, but upstream message taxonomy and wrapper preservation remain incomplete. | `compile_user_function_*failure*`, `compile_inline_foreach_failure_is_atomic_and_source_backed`, nested conversion atomicity, chain/conditional failure tests; independent upstream probes. | `PARTIAL` | #149/#167; #150; #63 corpus. | Reconcile upstream partial-effect semantics with the accepted Scribium atomicity invariant before implementation; do not patch the production bug in this audit. |
| Diagnostics, source provenance, deterministic AST -> evaluator -> IR behavior | Pinned call/refiner/expander/output sources above; ADR-0020; current engine diagnostics | Equivalent inputs produce deterministic diagnostics and IR; nested calls and bodies retain original Scribium source identity/spans; failures identify the owning stage. | Evaluation order is deterministic and no hidden global mutable runtime is used. | Provenance follows the call/body/argument owner through child scopes and outcome propagation. | No diagnostic or source state is stored as backend-specific runtime state. | Errors are structured and fail closed; unsupported semantics are not silently converted into successful output. | Rushdown/Quarkdown frontend preserves spans; #148 provenance gaps remain separate. | #149 conversion errors currently have a known generic-wrapper normalization gap (#167). | Engine owns structured diagnostics, evaluation limits, and IR normalization. | One backend-neutral `IrDocument`; no HIR/MIR/Typst escape variant introduced. | Pure lowering only consumes IR; backend behavior is not used to promote language support. | Existing source-backed `E3001`/call/body/conversion diagnostics and deterministic snapshots are evidence. | Diagnostic span tests, deterministic compilation tests, conformance fixtures, and `CallOutcome` tests. | `PARTIAL` | ADR-0015/0020; #148/#149; #63. | Complete upstream diagnostic wording/taxonomy and every nested conversion/body provenance path remain unverified; parser recovery belongs to #159. |
| Document-state separation from general programmable mutation | Current `DocumentState` in `scribium-engine`; [#152](https://github.com/luceat-lux-vestra/scribium/issues/152); ADR-0020 | Document metadata/state is a shared evaluator-owned state channel, not a general variable/callable scope contract. Its getter/setter/append/replace semantics are not re-inventoried here. | Callable scopes share the state handle, while variable ownership remains scoped. | State sharing is distinct from definition capture, caller overlay, parameter scope, and iteration-local scope. | Shared state commits are owned by the document-state workstream. | State conversion/commit failures link to #149/#167; broad metadata failures are #152. | Document metadata syntax/recognition is outside this audit. | #149 conversion is referenced where a state setter binds a target. | Engine owns the state channel; #150 does not introduce a second mutation model. | State snapshots remain backend-neutral serialized IR data. | Rendering/layout effects belong to #153/#154, not programmable runtime semantics. | Existing document-state diagnostics/evidence are linked, not duplicated. | `PROGRAMMABLE_DOCUMENT_SEMANTICS.md`, document-state tests/fixtures, `VALUE_MODEL_AUDIT.md`. | `NOT_APPLICABLE` | [#152](https://github.com/luceat-lux-vestra/scribium/issues/152); #149; existing #61/#131 evidence. | Reconcile shared-state interactions at #156; do not turn document metadata into a new general programmable-language issue. |

## Independent upstream black-box evidence

The following inputs were authored for this audit and run through the official
binary after verifying `quarkdown --version` as `quarkdown version 2.5.1`. The
release tag was independently resolved to the pinned commit above. The output
strings below omit only the binary's surrounding HTML paragraph wrapper.

| Probe | Observable result | Audit consequence |
|---|---|---|
| Recursive `.count` with `.if`/`.ifnot`, input `3` | `done` | Recursion is public and bounded; current Scribium has one independent semantic conformance test, but this does not complete the callable surface. |
| `.function {greet}` plus `.extend {greet}` and `.super::uppercase` | `HELLO WORLD` | `.extend` and `.super` are public semantic constructs; Scribium is `UNSUPPORTED`, tracked by #169. |
| Conditional extension `where:{name: .name::equals {World}}` invoked for `World` and `Other` | `HELLO WORLD`, then `Hello Other` | False extension conditions delegate to the prior callable; extension resolution is not a simple alias. |
| `.var {x} {before}`, callable writes `after`, then fails | `after` remains observable after the error | Upstream publishes mutation already performed before body failure; current Scribium rollback is a bounded divergence that needs post-#156 decision. |
| Ordered `.foreach` writes `1`, then `2`, then fails on item `2` | `2` remains observable after the error | Iteration failure has the same partial-effect observation; do not mark current atomic tests as upstream parity. |
| False `.if` with a failing body | `ok` only | Unselected branches are lazy. |
| Failed chain followed by an independent top-level sum | error, then `7` | Chain failure propagates without fabricating a value and does not prevent later independent evaluation. |

These probes are evidence of upstream observable behavior only. They are not
copied upstream tests and do not authorize changing Scribium's existing
failure-atomicity invariant in this audit PR.

## Existing evidence and cross-workstream reconciliation

- [#61](https://github.com/luceat-lux-vestra/scribium/issues/61) and [PR #131](https://github.com/luceat-lux-vestra/scribium/pull/131) are retained as evidence for the bounded evaluator foundation: callable bodies, caller overlay, owner write-back, explicit/implicit parameters, Pair destructuring, source/native precedence slices, atomicity tests, limits, one IR, diagnostics, and real Typst/PDF boundaries. The #61 closure audit explicitly does **not** claim complete v2.5.1 compatibility.
- [#148](https://github.com/luceat-lux-vestra/scribium/issues/148), [CALL_GRAMMAR_AUDIT.md](CALL_GRAMMAR_AUDIT.md), and #157–#164 own recognition, delimiter, source recovery, argument shape, and provenance gaps. #150 uses their parser evidence but does not create a parallel grammar inventory.
- [#149](https://github.com/luceat-lux-vestra/scribium/issues/149) and [VALUE_MODEL_AUDIT.md](VALUE_MODEL_AUDIT.md) own value taxonomy, binder behavior, target-driven conversion, conversion diagnostics, and conversion-related atomicity. #165–#167 remain the bounded implementation backlog; #150 records only their effect on call evaluation and scope ownership.
- [#151](https://github.com/luceat-lux-vestra/scribium/issues/151) owns the complete general stdlib inventory. Collection selectors, sorting, transforms, and callback invocation are included here only as core evaluation boundaries, without per-builtin issue fragmentation.
- [#152](https://github.com/luceat-lux-vestra/scribium/issues/152), [#153](https://github.com/luceat-lux-vestra/scribium/issues/153), [#154](https://github.com/luceat-lux-vestra/scribium/issues/154), and [#155](https://github.com/luceat-lux-vestra/scribium/issues/155) own document state, layout/configuration, content, and resource semantics respectively. [#24](https://github.com/luceat-lux-vestra/scribium/issues/24) remains the source-context/resource boundary; [#62](https://github.com/luceat-lux-vestra/scribium/issues/62) and [#63](https://github.com/luceat-lux-vestra/scribium/issues/63) are historical resource/corpus work, not duplicate #150 ownership.
- [#56](https://github.com/luceat-lux-vestra/scribium/issues/56) and [#60](https://github.com/luceat-lux-vestra/scribium/issues/60) are closed M2 tracking/syntax issues; their closure does not promote this semantic inventory.
- No existing issue covered public `.extend`/`.super` evaluator semantics. The cohesive follow-up [#169](https://github.com/luceat-lux-vestra/scribium/issues/169) records that gap without implementation. No issues were created for individual builtins, tests, or rows.

## Status and implementation boundary

The current canonical status is conservative: the callable, scope, argument,
evaluation-order, chain, conditional, iteration, collection, precedence,
failure, and diagnostic groups are `PARTIAL`; extension/super is
`UNSUPPORTED`; document-state separation is `NOT_APPLICABLE` to this audit.
No row is `SUPPORTED_END_TO_END`. The new recursion test strengthens evidence
for a bounded behavior but does not change the family-level `PARTIAL` claim.

This audit PR contains no production evaluator, parser, builtin, conversion,
IR, backend, dependency, or runtime-abstraction change. Its only code change
is the independent test named above. Implementation ordering remains frozen
until #156 completes the evidence/conformance/backlog reconciliation; #169 and
#157–#167 are follow-up backlog only.

## Residual review questions

The following evidence is intentionally unresolved rather than guessed:

- whether the full v2.5.1 public callable/component surface has additional
  callback/body consumers beyond the audited source families;
- the complete upstream commit/rollback contract for every stateful builtin,
  especially where existing Scribium atomicity is stricter;
- whether all public truth/None and dynamic/content conversion variants are
  observable through the same paths, which remains #149/#166 ownership;
- complete extension-chain failure, mutation, and diagnostic behavior for
  `.extend`/`.super`, deferred to #169 after #156; and
- full rendered-document and backend evidence for the partial semantic rows,
  which remains #63/conformance and the later workstreams rather than an
  implicit #150 claim.
