# Container combinations

> A quote can contain a list:
>
> - quoted item
>   - quoted nested item
>
>     ```text
>     quoted code
>     ```

- outer item
  - middle item
    - deep item
  - another item

1. ordered item
   > blockquote inside list
   >
   > - quoted list
